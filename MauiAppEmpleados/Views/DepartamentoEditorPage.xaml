<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppEmpleados.Views.DepartamentoEditorPage"
             Title="{Binding Title}">
    <VerticalStackLayout Spacing="20" Padding="30">

        <Label Text="Nombre del Departamento" FontAttributes="Bold" FontSize="16"/>
        <Entry Text="{Binding Departamento.Name}" Placeholder="Ej: Recursos Humanos"/>

        <Grid ColumnDefinitions="*,*" ColumnSpacing="15" Margin="0,30,0,0">
            <Button Grid.Column="0" Text="Cancelar" Command="{Binding CancelCommand}" BackgroundColor="Gray"/>
            <Button Grid.Column="1" Text="Guardar" Command="{Binding SaveCommand}" BackgroundColor="#107C10"/>
        </Grid>

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"/>

    </VerticalStackLayout>
</ContentPage>
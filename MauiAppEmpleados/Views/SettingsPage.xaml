<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppEmpleados.Views.SettingsPage"
             Title="{Binding Title}">
    <StackLayout Padding="20" Spacing="20">
        <Label Text="Ajustes de la Aplicación" FontSize="24" />

        <Label Text="Tema de la Interfaz:" TextColor="Gray"/>

        <Picker ItemsSource="{Binding Themes}" 
                SelectedItem="{Binding SelectedTheme}"
                Title="Selecciona un tema"/>

        <Button Text="Guardar Preferencias (Simulado)" BackgroundColor="{StaticResource Primary}"/>

        <Button Text="Acerca de..." Command="{Binding OpenAboutCommand}" />
    </StackLayout>
</ContentPage>